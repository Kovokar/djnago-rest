-- 2. Listagem contendo os nomes das categorias, ordenados alfabeticamente. 
-- A coluna de nomes deve ter a palavra ‘Categoria’ como título.

select nome AS Categoria3 from categoria c order by nome
